# פרומפט סשן אפיון — עצמאי (בלי תלות בקבצי הענף)

שקד מדביק את **כל הבלוק** בצ׳אט Cursor חדש. החומר בתוך הפרומפט. אל תחפש PDF בגיט.

```
סשן אפיון Windsage. אל תכתוב קוד. אל תמזג PRs. אל תריץ eas. אל תדרוס store.json.

הקבצים docs/SHAKED_DECISION_BRIEF_2026-08-21.pdf וכו' חיים רק בענף
cursor/weekly-progress-review-5b2b (לא ב-main). צופה PDF ב-Cursor נפתח ריק.
אל תחפש אותם. החומר למטה הוא המקור לסשן הזה.

צעד 1 — הצג לשקד עכשיו את דוח המחליט (העתק לצ׳אט בעברית, כמו PDF). אל תסכם במקום זה. בקש ממנו לאשר שקרא, ואז עבור לאפיון.

===== התחלת דוח המחליט =====

Windsage · דוח למחליט · 21 באוגוסט 2026
עקרונות והמלצות לשקד (מנהל פיתוח, יזם, מחליט)
יעד: 100 משתמשים. חבורה שמזמינה את החבר הבא. דיסקורד = מועדון. הטלפון = פעמון.

מה אתה מחליט, לא מה הסוכן כותב. צוות ייעוץ = לקוח + בקרה מול LLM.

1. משפט החזון
קבוצת חברים בונה שכבה שאומרת מתי לנו שווה לקום — לא מחליפה Windguru / Windy / KDI.
אם זה מגניב, דן מבקש הזמנה לדיסקורד. יעד: 100 משתמשים, לא מיליון ולא שלושה.

2. עקרונות לגזירת אפיונים
1) חבורה קודם. הצלחה = הזמנה, לא הורדה בחנות.
2) לא מתחרים. מקשרים ל-KDI / Windguru / מצלמה. אסור מסך "תחליף ל-Windy".
3) הטלפון ישן. הענן בודק. ההפרעה היחידה: זה מחזיק.
4) החזקה, לא דקירה. "זרקה 16" = רעש. "16 קשר × 20 דקות" = סשן.
5) שמות חופים. הרצליה / פריגול, לא 2259 למשתמש.
6) סף אישי. לא שידור 16 קשר לכולם כמו טלגרם KDI.
7) דיסקורד = מועדון; PWA = פעמון. שני לקוחות, מוח אחד (Wald).
8) בוט בלי מד טוקנים. שאלה → /v1 של Windsage → תשובה. אסור GPT לכל הודעה. Ollama מקומי רק לשפה חופשית.
9) עברית במועדון. אנגלית בקוד ובמזהים.
10) מוח אחד. store.json + פול. אין חנות נפרדת לבוט.
11) PR קטן. אסור למזג PR #9 כמו שהוא (ternary שובר Metro).
12) 100 משתמשים על JSON מסודר — כן. Postgres "כדי להיות רציניים" — לא.

3. ארכיטקטורה ל-100
- הכל JSON? כן. קובץ אחד על Wald: store.json + .bak. מזג אוויר חי ממקורות חיצוניים.
- מחזיק 100 משתמשים? כן, עם תור ו-cache. תהליך אחד, כתיבות בטור.
- מתי SQLite? רק אחרי טריגר: קובץ > ~5MB, פול > ~30 שנ', תור שמתארך, או היסטוריה לשאילתא. אותם נתיבי /v1.
- FE/BE? כן בגבול API, לא בשני מוצרים. Expo = לקוח. code/cloud = שרת. דיסקורד = לקוח שלישי של /v1. אל תפצל ריפואים.
- מה נשבר קודם בגידול? Wald אחד + WAN ביתי + Windguru — לא פורמט הקובץ.

4. דיסקורד
אין קוד בוט בגיט (נכון ל-21.8.2026). אם יש שרת — מחוץ לקוד עד webhook/BOT_TOKEN ב-oauth.env (לא בקומיט).
מינימום: webhook על Freegull/הרצליה בהחזקה + /רוח + /לאן בלי LLM.
קודם באזז בכיס.

5. לעשות החודש, לפי הסדר
1) באזז מסך נעול: Safari → מסך בית → טסט. webPush ב-/health כבר true.
2) דיסקורד כלקוח של /v1 בלי טוקני מודל.
3) קטלוג שמות עברית + מסך "לאן לצאת".
4) מ-PR #9 לפצל רק: חיפוש שם + אי-מחיקת follows. לא 69 הקבצים.
5) קרון גיבוי ל-store.json.

לא עכשיו: חנות אפל/גוגל, מייל למשתמש, Facebook SSO, Postgres, "מי בחוץ", מפת Windy, GPT לכל הודעת דיסקורד, מיזוג PR #9 המלא.

6. מדד הצלחה ל-100
לא MAU. 100 חשבונות שמקבלים החזקה בכיס ובמועדון, בעברית, בלי שהקובץ ייפול בפול. חבר מחוץ לגרעין מבקש הזמנת דיסקורד.

חי: https://windsage.nimrod.bio/   https://windsage.nimrod.bio/health

===== סוף דוח המחליט =====

אחרי ששקד אישר שקרא — אפיון בעברית:

תפקידים: אנחנו הלקוח + בקרה מקצועית מול LLM. שקד המחליט. אתה הספק.
לכל עקרון 1–12 ולכל R למטה: מקבל / מקבל-חלקית / דוחה / לא-עכשיו + נימוק + ראיה מהקוד או מ-/health.
דחייה מנומקת = ציון גבוה. חתימה עיוורת = כשל.
אל תמציא ש-Discord בריפו או ש-PR #9 ממוזג.

R-CLUB חבורה קודם, הזמנה לא חנות
R-NOCOMP לא מתחרים, רק קישורים
R-HE עברית למשתמש
R-HOLD באזז על החזקה לא דקירה
R-SLEEP הטלפון לא פולל ברקע
R-NAMES שמות חופים לא מזהי תחנה
R-PERSONAL סף per-follow
R-PUSH Web Push למסך נעול חייב הוכחה
R-DISCORD שרת שקד = מועדון
R-BOT0 בוט מ-/v1, אפס טוקני LLM
R-BOT1 שפה חופשית רק מקומית / מיפוי מילים
R-ONEBRAIN מוח אחד
R-JSON store.json ל-100
R-API גבול /v1, בלי פיצול ריפו
R-NOWIPE אסור דריסת users/stations
R-PR9 אסור מיזוג #9 המלא
R-NOEAS בלי eas/חנות
R-EXPO57 תיעוד SDK 57 בלבד
R-100 JSON מספיק ל-100; SQLite רק אחרי טריגר

רעיונות לגזירת מסכים (אחרי העקרונות): לאן לצאת; כיס+מועדון על אותו shouldNotify; כניסה מול החזקה בדיסקורד; כפתור אני יוצא; /רוח /לאן /סף /יוצא /בריאות בלי LLM; מילון ספוטים עברית.

פלט: מסמך אפיון בעברית — טבלת עמדות, מסכים מוצעים (PWA / בוט / כללים), סדר בניה לשבוע, מה לא בונים.
בדוק גם https://windsage.nimrod.bio/health מול הקוד ב-checkout שיש לך.
```
